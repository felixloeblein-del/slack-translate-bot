# Copy to .env and fill in values. Do not commit .env.

# Slack app (api.slack.com/apps): Basic Information -> App Credentials
SLACK_SIGNING_SECRET=your_signing_secret
# Slack app: OAuth & Permissions -> Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-bot-token

# DeepL API key (developers.deepl.com)
DEEPL_API_KEY=your_deepl_api_key

# Optional: only translate in these channel IDs (comma-separated). Empty = all channels.
# SLACK_CHANNEL_IDS=C01234ABCD,C56789EFGH

# Optional: reject requests older than this (seconds). Default 300.
# SLACK_REQUEST_MAX_AGE_SECONDS=300

# Optional: when to translate. "all" = every message (default). "prefix" = only if message starts with TRANSLATE_PREFIX. "mention" = only if message @mentions the bot.
# TRANSLATE_TRIGGER=all
# TRANSLATE_TRIGGER=prefix
# TRANSLATE_TRIGGER=mention

# Optional: for TRANSLATE_TRIGGER=prefix, only translate messages starting with this (prefix is stripped). Default [translate]
# TRANSLATE_PREFIX=[translate]

# Optional: for TRANSLATE_TRIGGER=reaction, only translate when this emoji is added to a message. Use shortcode without colons (e.g. de for :de:). Default de
# REACTION_TRIGGER_EMOJI=de

# Optional: for reaction-on-thread-reply in public/private channels. Slack only allows user tokens to
# fetch thread replies in channels (bot token works in DMs only). Add User Token Scopes channels:history
# (and groups:history for private channels) in OAuth & Permissions, reinstall the app, then set this
# to the "User OAuth Token" (starts with xoxp-) from OAuth & Permissions.
# SLACK_USER_TOKEN=xoxp-your-user-token

# Optional: if the message contains any of these phrases, only the text *after* the phrase is translated (preamble skipped). Comma-separated. Default includes: Can you please assist us with a translation of the following:, Can you translate the following:, Please translate the below:, translation of the following:, the following:
# EXTRACT_CONTENT_AFTER=Can you please assist us with a translation of the following:,Can you translate the following:,Please translate the below:,translation of the following:,the following:
